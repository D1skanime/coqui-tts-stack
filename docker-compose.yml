version: "3.9"
services:
  tortoise-tts:
    build: .
    container_name: tortoise-tts
    ports:
      - "5003:5003"
    volumes:
      - /volume1/docker/ki/coqui/models:/root/.cache
    restart: unless-stopped
