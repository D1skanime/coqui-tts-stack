version: "3.9"

services:
  coqui-tts:
    build: .
    container_name: coqui-tts
    ports:
      - "5002:5002"
    volumes:
      - /volume1/docker/ki/coqui/models:/home/tts/.local/share/tts
    restart: unless-stopped
